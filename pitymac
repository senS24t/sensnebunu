
local PlaceID = game.PlaceId
local AllIDs = {}
local foundAnything = ""
local actualHour = os.date("!*t").hour
local Deleted = false
local File = pcall(function()
    AllIDs = game:GetService('HttpService'):JSONDecode(readfile("NotSameServers.json"))
end)
if not File then
    table.insert(AllIDs, actualHour)
    writefile("NotSameServers.json", game:GetService('HttpService'):JSONEncode(AllIDs))
end
function TPReturner()
    local Site;
    if foundAnything == "" then
        Site = game.HttpService:JSONDecode(game:HttpGet('https://games.roblox.com/v1/games/2809202155/servers/Public?sortOrder=Asc&limit=100'))
    else
        Site = game.HttpService:JSONDecode(game:HttpGet('https://games.roblox.com/v1/games/2809202155/servers/Public?sortOrder=Asc&limit=100'))
    end
    local ID = ""
    if Site.nextPageCursor and Site.nextPageCursor ~= "null" and Site.nextPageCursor ~= nil then
        foundAnything = Site.nextPageCursor
    end
    local num = 0;
    for i,v in pairs(Site.data) do
        local Possible = true
        ID = tostring(v.id)
        if v.playing ~= v.maxPlayers then
            for _,Existing in pairs(AllIDs) do
                if num ~= 0 then
                    if ID == tostring(Existing) then
                        Possible = false
                    end
                else
                    if tonumber(actualHour) ~= tonumber(Existing) then
                        local delFile = pcall(function()
                            delfile("NotSameServers.json")
                            AllIDs = {}
                            table.insert(AllIDs, actualHour)
                        end)
                    end
                end
                num = num + 1
            end
            if Possible == true then
                table.insert(AllIDs, ID)
                wait()
                pcall(function()
                    writefile("NotSameServers.json", game:GetService('HttpService'):JSONEncode(AllIDs))
                    wait()
                    game:GetService("TeleportService"):TeleportToPlaceInstance(PlaceID, ID, game.Players.LocalPlayer)
                end)
                wait(0)
            end
        end
    end
end

function Teleport()
    while wait(0) do
        pcall(function()
            TPReturner()
            if foundAnything ~= "" then
                TPReturner()
            end
        end)
    end
end

getgenv().dev = true
local function nebun()
    task.spawn(function() pcall(function()
        game:GetService("CoreGui").DescendantAdded:Connect(function(child) 
            if child.Name == "ErrorPrompt" then 
                local GrabError = child:FindFirstChild("ErrorMessage",true)
                repeat wait() until GrabError.Text ~= "Label" 
                local Reason = GrabError.Text 
                if Reason:match("kick") or Reason:match("You") or Reason:match("conn") or Reason:match("rejoin") or Reason:match("kicked") then
                         wait(0.50)
                 game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer);
                end
            end
        end)
        end)
        end)
    end
    nebun()


timer = 20
nir = 10


repeat wait(1) 
nebun()
    print("coaee")

until game:IsLoaded() and game.Players.LocalPlayer.PlayerGui and workspace.Living:WaitForChild(game.Players.LocalPlayer.Name) or timer == 1



_G.Ver = "2.4"
repeat wait()
    if game.Players.LocalPlayer.PlayerGui:FindFirstChild("LoadingScreen1") and game:GetService("Players").LocalPlayer.PlayerGui.LoadingScreen1.Frame.LoadingFrame.BarFrame.Progress.Text ~= "LOADING ASSETS 100/100" then
        wait(1)
        nir = nir - 1
        print("nir"..nir)
end
until game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("LoadingScreen1") == nil or nir == 0
if nir == 0 then
    game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
end
repeat wait()
    if game.Players.LocalPlayer.PlayerGui:FindFirstChild("LoadingScreen")~=nil and game.Players.LocalPlayer.PlayerGui:FindFirstChild("LoadingScreen1")==nil then

                 for i,v in pairs(getconnections(game.Players.LocalPlayer.PlayerGui:WaitForChild("LoadingScreen"):WaitForChild("Frames"):WaitForChild("Gamemodes"):WaitForChild("Main").MouseButton1Click)) do
   v:Fire()
end
end
until game.Players.LocalPlayer:FindFirstChild("LoadingScreen")==nil or nir == 0
if nir == 0 then
    game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
end
if game.Players.LocalPlayer:FindFirstChild("LoadingScreen")==nil then
wait(0.50)


old = hookmetamethod(game,"__namecall", function(self,...)
    local args = {...}
    local method = getnamecallmethod()
    if tostring(self) == "Returner" and tostring(method) == "InvokeServer" then
        return "  ___XP DE KEY"
    end
    return old(self, ...)
end)

        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-300.0344543457031, 1.3260160684585571, -809.02490234375)


end

if workspace:FindFirstChild("Arrow")==nil then
    local hi = Instance.new("NumberValue")
    hi.Parent = game.Workspace
    hi.Name = ("Arrow")
    print("createda")
end


if game:GetService("CoreGui"):FindFirstChild("HsB Wm")==nil then
local GUI = Instance.new("ScreenGui")
    local Main = Instance.new("Frame", GUI)
    local title = Instance.new("TextLabel", Main)
    local message = Instance.new("TextLabel", Main)
    local pula = Instance.new("TextLabel", Main)
    local niager = Instance.new("TextLabel", Main)
    GUI.Name = "HsB Wm"
    GUI.Enabled = true
    GUI.Parent = game.CoreGui
    title.Name = ("Buci")
    message.Name = ("Bucu")
    pula.Name = ("Nuvreau")
    niager.Name = ("Bine")
    Main.Name = "MainFrame"
    Main.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
    Main.BorderSizePixel = 0
    Main.Position = UDim2.new(0, 0, 0, -40)
    Main.Size = UDim2.new(1, 0, 0, 1100)
    Main.BackgroundTransparency = 1


    title.BackgroundTransparency = 1
    title.Font = Enum.Font.SourceSansBold
    title.Position = UDim2.new(0, -30, 1, -1100)
    title.Size = UDim2.new(1, 0, 0, 120)
    title.Text = "Made by senS#5847"
    title.TextColor3 = Color3.new(455, 455, 455)
    title.TextSize = 17
    title.TextScaled = true
    title.TextTransparency = 1

    message.BackgroundTransparency = 1
    message.Font = Enum.Font.SourceSansBold
    message.Position = UDim2.new(0, -30, 1, -900)
    message.Size = UDim2.new(1, 0, 0, 60)
    message.Text = "Join Discord for updates/bug reports"
    message.TextColor3 = Color3.new(455, 455, 455)
    message.TextSize = 17
    message.TextScaled = true
    message.TextWrapped = true
    message.TextTransparency = 1
    
    pula.BackgroundTransparency = 1
    pula.Font = Enum.Font.SourceSansBold
    pula.Position = UDim2.new(0, -30, 1, -500)
    pula.Size = UDim2.new(1, 0, 0, 70)
    pula.Text = "discord.gg/HTCZPzTrzc"
    pula.TextColor3 = Color3.new(455, 455, 455)
    pula.TextSize = 20
    pula.TextScaled = true
    pula.TextWrapped = true
    pula.TextTransparency = 1

    niager.BackgroundTransparency = 1
    niager.Font = Enum.Font.SourceSansBold
    niager.Position = UDim2.new(0, -30, 1, -810)
    niager.Size = UDim2.new(1, 0, 0, 60)
    niager.Text = "Invite Copied"
    niager.TextColor3 = Color3.new(455, 455, 455)
    niager.TextSize = 17
    niager.TextScaled = true
    niager.TextWrapped = true
    niager.TextTransparency = 1

setclipboard(tostring(pula.Text))
print(tostring(pula.Text))





    local TweenService = game:GetService('TweenService')
    local Frame = game.CoreGui["HsB Wm"].MainFrame
    local unu = Frame.Buci
    local doi = Frame.Bucu
    local trei = Frame.Nuvreau
    local patru = Frame.Bine
        
    TweenService:Create(
        Frame, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {BackgroundTransparency = 0} -- Goal
    ):Play()

    TweenService:Create(
        unu, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 0} -- Goal
    ):Play()

    TweenService:Create(
        doi, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 0} -- Goal
    ):Play()

    TweenService:Create(
        doi, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 0} -- Goal
    ):Play()

    TweenService:Create(
        trei, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 0} -- Goal
    ):Play()

    TweenService:Create(
        patru, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 0} -- Goal
    ):Play()

    wait(3)

    TweenService:Create(
        Frame, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {BackgroundTransparency = 1} -- Goal
    ):Play()

    TweenService:Create(
        unu, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 1} -- Goal
    ):Play()

    TweenService:Create(
        doi, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 1} -- Goal
    ):Play()

    TweenService:Create(
        doi, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 1} -- Goal
    ):Play()

    TweenService:Create(
        trei, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 1} -- Goal
    ):Play()

    TweenService:Create(
        patru, -- UI object you're tweening, in this case it's Frame
        TweenInfo.new(0.5), -- Amount of seconds
        {TextTransparency = 1} -- Goal
    ):Play()
end

for i,v in pairs(game:GetService("Workspace").Spawns:GetDescendants()) do
    
    if v.Name == ("SpawnLocation") then
        if getgenv().HidingSpot == 1 then
            v.CFrame = CFrame.new(582.705322265625, -35.1239128112793, -272.3077392578125)
        end
        if getgenv().HidingSpot == 2 then
            v.CFrame = CFrame.new(1737.0465087890625, -42.090232849121094, -1576.7703857421875)
        end
    end
end

local function rasa(titlu, content)
    local webhookcheck =
       is_sirhurt_closure and "Sirhurt" or pebc_execute and "ProtoSmasher" or syn and "Synapse X" or
       secure_load and "Sentinel" or
       KRNL_LOADED and "Krnl" or
       SONA_LOADED and "Sona" or
       "Kid with shit exploit"
    
    local url =
       "https://discord.com/api/webhooks/1035908072486486036/qHhW7tGsFaAArHXCOT7OHAwA0X5acpAT0O8TKZT5IWZueXYRXggkoFo-9hWqCmB87q2p"
    local data = {
       ["content"] = content,
       ["embeds"] = {
           {
               ["title"] = titlu,
               ["description"] = "**Username**: " .. game.Players.LocalPlayer.Name.." with **"..webhookcheck.."** on "..game.PlaceId,
               ["type"] = "rich",
               ["color"] = tonumber(0x7269da),
               ["image"] = {
                   ["url"] = "http://www.roblox.com/Thumbs/Avatar.ashx?x=150&y=150&Format=Png&username="..tostring(game:GetService("Players").LocalPlayer.Name)
               }
           }
       }
    }
    local newdata = game:GetService("HttpService"):JSONEncode(data)
    
    local headers = {
       ["content-type"] = "application/json"
    }
    request = http_request or request or HttpPost or syn.request
    local abcdef = {Url = url, Body = newdata, Method = "POST", Headers = headers}
    request(abcdef)
    end



nebun()
local function izi()
    print("okay")
end


    if workspace:FindFirstChild("Para")==nil then
        local hi = Instance.new("BillboardGui")
        hi.Parent = game.Workspace
        hi.Name = ("Para")
        print("createdf2")
        end

    if game.CoreGui:FindFirstChild("Bine11")==nil then
        local nu = Instance.new("ScreenGui")
        local nuu = Instance.new("TextLabel",nu)
        local nana = Instance.new("TextLabel",nu)
        nu.Parent = game.CoreGui
        nu.Name = "Bine11"
        nu.Enabled = false
        nuu.Font = "SourceSansBold"
        nuu.Size = UDim2.new(1, 0, 1, 0)
        nuu.Text = "Pity Chance: "
        nuu.TextSize = 60
        nuu.Position = UDim2.new(0, -750, 0, -200)
        nuu.BackgroundTransparency = 1
        nuu.TextColor3 = Color3.new(76, 0, 114)
        nuu.TextScaled = false
        nuu.TextStrokeColor3 = Color3.new(455, 455, 455)
        nuu.TextStrokeTransparency = 0

        nuu.Name = "nuu"
        nana.Font = "SourceSansBold"
        nana.Name = "nana"
        nana.Size = UDim2.new(1,0,1,0)
        nana.Text = ""
        nana.TextColor3 = Color3.new(122, 17, 215)
        nana.TextScaled = false
        nana.TextSize = 70
        nana.Position = UDim2.new(0, -540, 0, -200)
        nana.TextStrokeColor3 = Color3.new(0, 0, 0)
        nana.TextStrokeTransparency = 0
        nana.BackgroundTransparency = 1

    end

if getgenv().dev == true then
    
local bu = Instance.new("BillboardGui")
bu.Parent = workspace
bu.Name = "nuvr"
print("dev")
end

    print(tostring("Current Version: ".._G.Ver))

    local count = 0
    local time = 14
    local timer = 20
    local lp = game.Players.LocalPlayer

local learnskill = "LearnSkill"
local tablaskill = {
	["Skill"] = "Worthiness V",
	["SkillTreeType"] = "Character"
}
local activstand = "ToggleStand"
local standac = "Toggle"

local runServ = game:GetService("RunService")
    local GoldCoin = "rbxassetid://7124126253"
    local Arrow = "rbxassetid://7106104343"
    local Glove = "rbxassetid://7138936196"
    local Roka = "rbxassetid://7218405255"
    local redcandy = "rbxassetid://7747232080"
    local bluecandy = "rbxassetid://7747232080"
    local greencandy = "rbxassetid://7747232080"
    local yellowcandy = "rbxassetid://7747232080"
    local pureroka = ""
    local diamond = "rbxassetid://875644059"
    local stone = "rbxassetid://4551120971"
    local rib = 'rbxassetid://5249254947'
    local zep = "rbxassetid://6924442799"
    local diodiary = "rbxassetid://5675321032"
    local steelball = "rbxassetid://5291254518"
    local ancientscroll = "60791940"
    local lucky = "rbxassetid://7106118765"
    

    rasa(game.Players.LocalPlayer.Name.. " Successfully Executed Pity Farm", "Mine si minori, adica mineri, scuze")

repeat wait(2) until game.Players.LocalPlayer:FindFirstChild("Backpack")
local Children = {}
local function updarrow()
    for _, Obj in pairs(game.Players.LocalPlayer:WaitForChild("Backpack"):GetChildren()) do
        if Obj.Name == ("Rib Cage of The Saint's Corpse") then
            table.insert(Children, Obj)
        if workspace:FindFirstChild("Arrow") then workspace.Arrow.Value = #Children
end
    end
end
end

local function hop()
    local sl = game.HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/2809202155/servers/Public?limit=100"))
                for i,v in pairs(sl.data) do
                   if v.playing ~= v.maxPlayers then
                       game:service'TeleportService':TeleportToPlaceInstance(game.PlaceId, v.id)
                   end
                end
            end


updarrow()
wait(0.25)
print(workspace.Arrow.Value.." Arrow")

local nuvreau = {
    rib,
}

if getgenv().DoubleInventory == false then
if workspace.Arrow.Value == 10 then

    table.remove(nuvreau, 1)

end
end

if getgenv().DoubleInventory == true then
    if workspace.Arrow.Value == 20 then
        table.remove(nuvreau, 1)
    end
end

    _G.ok = true
    
    local lpcf = game.Players.LocalPlayer.Character.HumanoidRootPart

    if workspace.Arrow.Value == 10 then
        print("createheh")
    if workspace:FindFirstChild("nuvr")==nil then
        local ba = Instance.new("BillboardGui")
        ba.Name = ("nuvr")
        ba.Parent = workspace
    end
end
    local function mata()
        wait(1)
        time = time -1
        izi()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-300.0344543457031, 1.3260160684585571, -809.02490234375)
        print(time)


        if time == 0 then
            while wait(0.25) do

            
                timer = timer - 1
                print(timer)
                Teleport()

                if timer == 0 then
                    break
                end
                    repeat wait()
                        game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
                    until game.Players.LocalPlayer==nil
                    end
                end
            end
        
    




    local function tactu()
        
    for i,v in pairs(nuvreau) do
    for k,b in pairs(workspace.Item_Spawns.Items:GetChildren()) do
        if b:IsA("Model") then
            for i2,val in pairs(b:GetChildren()) do
                if (val:IsA("MeshPart" or val:IsA("Part"))) and val.Transparency ~=1 and val.MeshId == v then
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = val.CFrame
    wait(0.25)
        if val and (val.Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).magnitude <= 6 then
    
            if b:FindFirstChild("ProximityPrompt") then
                fireproximityprompt(b.ProximityPrompt)
wait(.30)
        --rasa("Ti-a picat "..val.MeshId, "Item Picat <@880922246644920360>, <@986257741926514718>. Versiunea: ".._G.Ver)
        warn(val.MeshId)
        wait(.25)
    if (game.Players.LocalPlayer.PlayerGui:FindFirstChild("Message") and game.Players.LocalPlayer.PlayerGui.Message:FindFirstChild("TextLabel")) and val and (val.Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).magnitude <= 5 then
        if string.match(lp.PlayerGui.Message.TextLabel.Text, "Rib Cage of The Saint's Corpse") then
            table.remove(nuvreau, 1)
        print("Removed".. val.MeshId)
        while wait() do
                                        hop()
        end
        elseif string.find(lp.PlayerGui.Message.TextLabel.Text, "Rokakaka") and not string.find(lp.PlayerGui.Message.TextLabel.Text, "Pure") then
            table.remove(nuvreau, 2)
        print("Removed"..val.MeshId)
        while wait() do
                                        hop()
        end
        end
            b:Destroy()
        end
    end
    end
end
end
end
end
end
end
            
    
    
    
    
local function nufrate()
    print("colo")
    while wait() do
            
        if #workspace.Item_Spawns.Items:GetChildren() == 0 then
            repeat wait(0.60) 
                

                mata()
            
            until #game:GetService("Workspace")["Item_Spawns"].Items:GetChildren() > 0 
        end
        
        if #game:GetService("Workspace")["Item_Spawns"].Items:GetChildren() > 0  then

    for i,aga in pairs(workspace.Item_Spawns.Items:GetChildren()) do
        if aga:IsA("Model") then
    for i2,aga2 in pairs(aga:GetChildren()) do
        for i,v in pairs(nuvreau) do
        if aga2:IsA("MeshPart") then
            
            if aga2.Transparency == 1 or aga2.MeshId ~= v then
                wait(0.25)
mata()

          
            elseif aga2.MeshId == v and aga2.Transparency~= 1 then
    
break
end
end
    tactu()

end
end
end
end
end
end
end

local function haim()
while wait() do
    
    lp.Character.RemoteFunction:InvokeServer(learnskill, tablaskill)

end
end

local function haima()

    lp.Character.Head:Destroy()


    workspace.Living.ChildAdded:connect(function(a)
        if a.Name == game.Players.LocalPlayer.Name then
            game:GetService("Players").LocalPlayer.PlayerGui:WaitForChild("HUD"):WaitForChild("Main"):WaitForChild("Frames"):WaitForChild("StandShop").Visible = false
             wait(1.55)

            firesignal(game:GetService("Players").LocalPlayer.PlayerGui.HUD.Main.Frames:WaitForChild("Store"):WaitForChild("ToggleStandShop"):WaitForChild("Button").MouseButton1Click)
            game:GetService("Players").LocalPlayer.PlayerGui:WaitForChild("HUD"):WaitForChild("Main"):WaitForChild("Frames"):WaitForChild("StandShop").Visible = false
lpcf = CFrame.new(19.791584014892578, -42.00715255737305, -1212.7635498046875)
game:GetService("CoreGui").Bine11.Enabled = true


if lp.PlayerStats.Stand.Value ~= "None" then
    
    lp.Character:WaitForChild("RemoteFunction"):InvokeServer(activstand, standac)

end
print('acumm')
local Finished = false
    repeat task.wait()
        firesignal(game:GetService("Players").LocalPlayer.PlayerGui.HUD.Main.Frames:WaitForChild("Store"):WaitForChild("ToggleStandShop"):WaitForChild("Button").MouseButton1Click)
        game:GetService("CoreGui").Bine11.nana.Text = game:GetService("Players").LocalPlayer.PlayerGui:WaitForChild("HUD"):WaitForChild("Main"):WaitForChild("Frames"):WaitForChild("StandShop"):WaitForChild("SkinChances1").Text

    local Children = {}
    for _, Obj in pairs(game.Players.LocalPlayer:WaitForChild("Backpack"):GetChildren()) do
        if Obj.Name == ("Rib Cage of The Saint's Corpse") then
            table.insert(Children, Obj)
        if workspace:FindFirstChild("Arrow") then workspace.Arrow.Value = #Children
end
    end
end
    
        lp.Character:WaitForChild("RemoteFunction"):InvokeServer(learnskill, tablaskill)
            if lp["PlayerStats"].Stand.Value == ("None") then
                print("ain")
                

                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1737.0465087890625, -42.090232849121094, -1576.7703857421875)
                
                print("1")
                if lp:WaitForChild("Backpack"):WaitForChild("Rib Cage of The Saint's Corpse") then
                    print("arro")
                    lp.Backpack:WaitForChild("Rib Cage of The Saint's Corpse").Parent = lp.Character
                    print("gotaro")
    lp.Character:WaitForChild("Rib Cage of The Saint's Corpse"):Activate()
                print("actiaro")


local nuuu = "EndDialogue"
local daaa = {
	["NPC"] = "Rib Cage of The Saint's Corpse",
	["Option"] = "Option1",
	["Dialogue"] = "Dialogue2"
}

game.Players.LocalPlayer.Character.RemoteEvent:FireServer(nuuu, daaa)
end
for i,v in pairs(lp.Character:GetChildren()) do
    if v:IsA("Tool") then v.Parent = lp.Backpack
     end
end

if getgenv().AutoClick == true then

    mouse1click()
    mouse1click()
    
end
end

repeat wait() until lp["PlayerStats"].Stand.Value ~= "None"
print("a")


        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1737.0465087890625, -42.090232849121094, -1576.7703857421875)
    
firesignal(game:GetService("Players").LocalPlayer.PlayerGui.HUD.Main.Frames:WaitForChild("Store"):WaitForChild("ToggleStandShop"):WaitForChild("Button").MouseButton1Click)
        if game.Players.LocalPlayer.Character:FindFirstChild("StandMorph")==nil then
            game.Players.LocalPlayer.Character.RemoteFunction:InvokeServer(activstand, standac)
        
        end

--game:GetService("Workspace").Living.Zieme91676.StandMorph.StandSkin.Value

    if game:GetService("Players").LocalPlayer.PlayerGui:WaitForChild("HUD"):WaitForChild("Main"):WaitForChild("Frames"):WaitForChild("StandShop"):WaitForChild("SkinChances1").Text ~= "4%" or game:GetService("Players").LocalPlayer.PlayerGui:WaitForChild("HUD"):WaitForChild("Main"):WaitForChild("Frames"):WaitForChild("StandShop"):WaitForChild("SkinChances1").Text ~= "4.2%" then
            
        local nuuu = "EndDialogue"
        local daaa = {
            ["NPC"] = "Rib Cage of The Saint's Corpse",
            ["Option"] = "Option1",
            ["Dialogue"] = "Dialogue2"
        }
        
        game.Players.LocalPlayer.Character.RemoteEvent:FireServer(nuuu, daaa)

        print("plizz")
        firesignal(game:GetService("Players").LocalPlayer.PlayerGui.HUD.Main.Frames:WaitForChild("Store"):WaitForChild("ToggleStandShop"):WaitForChild("Button").MouseButton1Click)

        

for i,v in pairs(lp.Character:GetChildren()) do
            if v:IsA("Tool") then v.Parent = lp.Backpack
             end
        end
if getgenv().AutoClick == true then

    mouse1click()
    mouse1click()
    
end
end



until workspace.Arrow.Value <= 1 or game:GetService("Players").LocalPlayer.PlayerGui:WaitForChild("HUD"):WaitForChild("Main"):WaitForChild("Frames"):WaitForChild("StandShop"):WaitForChild("SkinChances1").Text == (getgenv().PityWanted)


print('haicoae')
while wait(4) do
    
hop()
end
end



end)
end
if workspace:FindFirstChild("nuvr")==nil then
    print('nunu')
        nufrate()
end


if workspace:FindFirstChild("nuvr") then
    haima()
end
